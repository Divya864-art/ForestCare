name: my_app
description: A new Flutter project.
publish_to: 'none' # Remove this if you're publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  firebase_auth: ^4.17.5
  cloud_firestore: ^4.17.5
  google_fonts: ^6.1.0

 
  
  image_picker: ^1.0.0
  geolocator: ^10.0.0
  path: ^1.8.0
  path_provider: ^2.0.9


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/forest_bg.jpg

